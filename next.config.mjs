/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep strict build defaults for confidence during launch.
  typescript: {
    ignoreBuildErrors: false,
  },
}

export default nextConfig
